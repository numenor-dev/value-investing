const config = {
  theme: {
    extend: {
      fontFamily: {
        sans: 'var(--font-geist-sans)',
        mono: 'var(--font-geist-mono)',
        noto: 'var(--font-noto-sans)',
      },
    },
  },
}

export default config